# Руководство по делегированию задач

## Принцип разделения ответственности

**Архитектор (Gemini/GPT-4):** Стратегия → ЧТО делать  
**Кодер (Claude Code):** Тактика → КАК делать

---

## Когда использовать Архитектора

### ✅ Задачи для Архитектора

**Планирование и анализ:**
- Анализ требований пользователя
- Создание технических планов
- Проектирование архитектуры
- Выбор технологий
- Проектирование API контрактов
- Планирование миграций БД

**Принятие решений:**
- Архитектурные паттерны
- Структура данных
- Масштабирование
- Performance оптимизация
- Рефакторинг стратегия

**Создание спецификаций:**
- API документация
- Схемы баз данных
- Техническое задание для Кодера
- Описание процессов

### ❌ Что НЕ делает Архитектор

- Не пишет код
- Не выполняет команды на сервере
- Не деплоит
- Не работает с логами
- Не перезапускает сервисы
- Не фиксит баги напрямую

---

## Когда использовать Кодера

### ✅ Задачи для Кодера

**Разработка:**
- Написание кода по ТЗ
- Создание API endpoints
- Реализация бизнес-логики
- Написание тестов

**Работа с БД:**
- Создание миграционных скриптов
- Выполнение миграций на продакшене
- Бэкапы БД
- Исправление данных (по плану)

**Деплой и операции:**
- Коммит и push в git
- Pull на продакшен сервере
- Перезапуск сервисов
- Проверка логов
- Health checks

**Отладка:**
- Диагностика проблем
- Чтение логов
- Исправление багов
- Тестирование исправлений

### ❌ Что НЕ делает Кодер

- Не принимает архитектурные решения
- Не меняет структуру БД без плана
- Не проектирует API без спецификации
- Не делает рефакторинг без согласования

---

## Правильный Workflow

### Сценарий 1: Добавление новой фичи

**Шаг 1: Пользователь → Архитектор**
```
Пользователь: "Нужно добавить возможность блокировать устройства"
```

**Шаг 2: Архитектор анализирует**
- Изучает snapshot проекта
- Анализирует текущую архитектуру
- Определяет необходимые изменения:
  - Новый API endpoint
  - Обновление БД схемы
  - Бизнес-логика

**Шаг 3: Архитектор создаёт план**
```markdown
## План реализации блокировки устройств

### Изменения БД
- Добавить колонку `status` в таблицу `registered_devices`
- Значения: 'active', 'pending', 'blocked'
- Дефолт: 'pending'

### API Endpoints
POST /ECK/API/DEVICE/BLOCK
Body: { device_uuid: string }
Response: { success: boolean }

POST /ECK/API/DEVICE/UNBLOCK
Body: { device_uuid: string }
Response: { success: boolean }

### Логика
1. Проверить существование устройства
2. Обновить статус
3. Вернуть результат
```

**Шаг 4: Архитектор → Пользователь**
```
Архитектор: "Вот план реализации... [план]. Подтверждаете?"
```

**Шаг 5: Пользователь подтверждает**
```
Пользователь: "Да, подтверждаю"
```

**Шаг 6: Архитектор → Кодер (через JSON)**
```json
{
  "target_agent": "production_server",
  "task_id": "add-device-blocking",
  "objective": "Реализовать функциональность блокировки устройств",
  "context": "Добавить возможность менять статус устройств через API",
  "steps": [
    "Создать миграционный скрипт для добавления колонки status",
    "Добавить API endpoint POST /ECK/API/DEVICE/BLOCK",
    "Добавить API endpoint POST /ECK/API/DEVICE/UNBLOCK",
    "Протестировать локально",
    "Развернуть на продакшен"
  ],
  "validation": [
    "Проверить что миграция выполнилась",
    "Проверить что API endpoints работают",
    "Проверить логи сервиса"
  ]
}
```

**Шаг 7: Кодер выполняет**
- Создаёт скрипт миграции
- Реализует endpoints
- Тестирует
- Деплоит
- Валидирует

---

### Сценарий 2: Критический баг

**Когда НЕ нужен Архитектор:**
```
Пользователь: "Сервис не запускается после обновления"
→ Прямо к Кодеру для диагностики и исправления
```

**Кодер:**
1. Проверяет логи
2. Находит проблему
3. Фиксит
4. Перезапускает
5. Проверяет

**Когда нужен Архитектор:**
```
Кодер: "Проблема в архитектуре, нужно переделывать подход"
→ Эскалация к Архитектору
```

---

### Сценарий 3: Рутинная операция

**НЕ нужен Архитектор:**
```
Пользователь: "Перезапусти сервис"
→ Прямо к Кодеру
```

```
Пользователь: "Посмотри логи за последний час"
→ Прямо к Кодеру
```

```
Пользователь: "Обнови код с GitHub"
→ Прямо к Кодеру
```

---

## Формат коммуникации

### От Архитектора к Кодеру

**Всегда JSON формат:**
```json
{
  "target_agent": "production_server | local_dev",
  "agent_environment": "Production Linux Server | Local Development",
  "command_for_agent": "implement_feature | fix_bug | deploy_code | run_migration",
  "task_id": "уникальный-идентификатор",
  "payload": {
    "objective": "Что нужно сделать",
    "context": "Почему и зачем",
    "steps": ["Шаг 1", "Шаг 2"],
    "validation_steps": ["Как проверить"],
    "expected_outcome": "Что должно получиться"
  }
}
```

### От Кодера к Пользователю

**Статус-репорт:**
```markdown
✅ Выполнено:
- Создан скрипт миграции
- Добавлены API endpoints
- Протестировано локально

🚀 Деплой:
- Код отправлен на GitHub
- Выполнена миграция на продакшене
- Сервис перезапущен

✅ Валидация:
- Логи чистые, ошибок нет
- API endpoints работают корректно
- Health check: OK
```

---

## Типичные ошибки

### ❌ Неправильно

**Пользователь сразу к Кодеру с запросом на новую фичу:**
```
"Claude, добавь возможность блокировать устройства"
```
**Проблема:** Кодер не должен принимать архитектурные решения

**Правильно:** → Сначала к Архитектору для планирования

---

**Пользователь к Архитектору с рутинной задачей:**
```
"Gemini, перезапусти сервис на продакшене"
```
**Проблема:** Архитектор не выполняет команды

**Правильно:** → Сразу к Кодеру

---

**Архитектор пишет код:**
```
Архитектор: "Вот готовый код [10000 строк]"
```
**Проблема:** Архитектор должен создавать ПЛАН, не код

**Правильно:** → Архитектор создаёт ТЗ, Кодер пишет код

---

## Best Practices

### 1. Используй snapshots для Архитектора
```bash
# Создать snapshot перед планированием
eck-snapshot

# Дать Архитектору snapshot для анализа
```

### 2. Всегда получай подтверждение пользователя
```
Архитектор: "Вот план... Подтверждаете?"
Пользователь: "Да" ← ОБЯЗАТЕЛЬНО!
Архитектор: [Генерирует команды для Кодера]
```

### 3. Используй чёткий формат команд
```json
// Не так:
"Claude, сделай что-нибудь с базой данных"

// А так:
{
  "target_agent": "production_server",
  "task_id": "db-migration-001",
  "objective": "Добавить колонку status в registered_devices",
  ...
}
```

### 4. Разделяй задачи по сложности

**Простая задача → Кодер:**
- "Перезапусти сервис"
- "Посмотри логи"
- "Обнови код"

**Средняя задача → Архитектор → Кодер:**
- "Добавь новый endpoint"
- "Измени схему БД"
- "Рефактори модуль"

**Сложная задача → Архитектор (детальное планирование) → Кодер:**
- "Переработай архитектуру"
- "Добавь новый микросервис"
- "Оптимизируй performance"

---

## Итого

**Золотое правило:**
```
Архитектор = Думай
Кодер = Делай
```

**Процесс:**
```
Идея → Архитектор → План → Пользователь → Подтверждение → Кодер → Реализация
```

**Исключения:**
```
Рутинные операции → Сразу Кодер
Критические баги → Сразу Кодер (с эскалацией к Архитектору если нужно)
```
